# MarketFlow Sample Service

This module is a non-business reference service for Milestone 0. It proves the shared Java build,
health probes, correlation handling, structured logging, metrics, tracing, container build, and
test conventions that later bounded-context services will adopt.

It owns no domain data and exposes no marketplace API. Do not evolve it into a business service.

