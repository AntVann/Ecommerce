apiVersion: v1
kind: Secret
metadata:
  name: marketflow-runtime-secrets
  namespace: marketflow-local
  labels:
    app.kubernetes.io/part-of: marketflow
    marketflow.io/managed-by: external-secret-or-local-operator
  annotations:
    marketflow.io/secret-source: "Create this Secret locally or sync it from an external secret manager"
type: Opaque
# Deliberately empty. Populate out-of-band; never commit values here.
stringData: {}
